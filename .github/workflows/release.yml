name: release

on:
  push:
    branches:
      - main
permissions:
  contents: write
  issues: write
  packages: write
  id-token: write
  attestations: write
jobs:
  release-please:
    permissions:
      contents: write
      issues: write
      pull-requests: write
    uses: ./.github/workflows/release-please.yml
